Новая лаба: реализация k-means вручную

количество кластеров: 2
каждая точка задана 
- $(x,y,z)$ - объект
- выборка объема n (случайная, независимая, повтрная ) из генеральной совокупности S
- выбираем 2 первых объекта - центры потенциальных кластеров.
- ищем расстояния от каждого объекта до каждого из центров: если расстояние меньше до данного центра, то относим к одному из кластеров
- метрика: $\sqrt{\sum x_i^2}$
- Получили первое приближение кластеров. Выполняем усреднение по каждой координате для каждого кластера и получаем 2 новых центров кластеров.
- Повторяем пересчитывания для всех точек (если не рассматривать оптимизации алгоритма).
- Условие остановки (не дается): стабилизация центров классов - изменение центра класса становится очень маленькой
- Как оцениваем качество стабилизации? 

На сегодняшнюю пару только это, но это не вся лабораторная. 

задача: поисследовать модель и посмотреть ее особенности

идея кластеризации - развести центры кластеров как можно дальше друг от друга.

Тестирование на файле diabetes.csv. Игнорируем целевой признак, хотя можем и сравнить результаты кластеризации с целевым признаком, но это не является частью лабораторной.

Не интерпретируем результаты кластеризации!!! Семантики у данных нет!

Лаба не имеет названия - будет фрагментом следущей лабораторной работы